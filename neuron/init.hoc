proc init() { local dtsav, temp
    finitialize(v_init)
    t = -1e10
    dtsav = dt
    dt = dtsav
    t = 0
    if (cvode.active()) {
        cvode.re_init()
    }	else {
        fcurrent()
    }
    frecord_init()
}

